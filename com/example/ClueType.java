package com.example;

public enum ClueType {
    EITHER, //specify that X is either y or z
    EXCLUSIVE, //specify that X is not y
    EXACTLY_ONE, //specify that X is y
    NEIGHBOR, //specify that X is next to y

}
